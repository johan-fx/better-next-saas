# =============================================================================
# APP CONFIGURATION
# =============================================================================
APP_NAME="Your App Name" # Also used for auth cookie storage
APP_DEFAULT_LOCALE="en" # Default locale for your app
NEXT_PUBLIC_APP_URL="http://localhost:3000" # Public URL accessible from browser
NODE_ENV="development"

# =============================================================================
# AUTHENTICATION
# =============================================================================
BETTER_AUTH_SECRET="your-32-character-secret-key-here" # Generate a secure random string
BETTER_AUTH_URL="http://localhost:3000" # Base URL of your app

EMAIL_VERIFICATION_TOKEN_EXPIRES_IN="24" # Token expiration in hours

# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL="postgresql://username:password@host:5432/database_name?sslmode=require"

# =============================================================================
# EMAIL SETTINGS (DEVELOPMENT)
# =============================================================================
# MailDev settings for local development email testing
MAILDEV_WEB_URL="http://localhost:1080"
MAILDEV_SMTP_HOST="localhost"
MAILDEV_SMTP_PORT="1025"
MAILDEV_USERNAME="test"
MAILDEV_PASSWORD="test"

# =============================================================================
# EMAIL SETTINGS (PRODUCTION)
# =============================================================================
# SMTP settings for production email sending
SMTP_HOST="smtp.example.com" # Your SMTP server hostname
SMTP_PORT="587" # SMTP port (587 for TLS, 465 for SSL)
SMTP_FROM="noreply@yourapp.com" # From email address
SMTP_USER="your-smtp-username" # SMTP authentication username
SMTP_PASS="your-smtp-password" # SMTP authentication password

# =============================================================================
# OAUTH PROVIDERS
# =============================================================================
# Google OAuth - Get credentials from https://console.developers.google.com
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# GitHub OAuth - Get credentials from https://github.com/settings/applications/new
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================
# Languine translation service - Get project ID from https://languine.ai
LANGUINE_PROJECT_ID="prj_your_project_id_here"

# Stripe payment processing - Get secret key from https://dashboard.stripe.com
STRIPE_SECRET_KEY="your-stripe-secret-key"
STRIPE_WEBHOOK_SECRET="your-stripe-webhook-secret"
NEXT_PUBLIC_STRIPE_ENABLED="false" # Set to "true" when server Stripe is configured